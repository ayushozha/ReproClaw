[project]
name = "reproclaw-api"
version = "0.1.0"
description = "Dependency-light backend scaffold for ReproClaw"
requires-python = ">=3.12"
dependencies = [
  "anthropic>=0.55",
  "fastapi>=0.110",
  "httpx>=0.27",
  "pypdf>=4",
  "python-multipart>=0.0.9",
  "uvicorn>=0.27",
]

[tool.uv]
package = false

